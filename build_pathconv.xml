<project name="webserver" basedir="." default="jar">
    
	<property name="build.dir" value="build" />
	<property name="jar.dir" value="."/>
	<property name="main-class" value="swx.stdapp.webserver.SimpleWebServer"/>

	<fileset id="files.jar" dir="lib">
		<include name="*.jar"/>
	</fileset>

	<pathconvert pathsep=" " property="manifest.classpath">
		<path>
			<fileset refid="files.jar"/>
		</path>
		<mapper>
			<chainedmapper>
			   <flattenmapper/>
			   <globmapper from="*.jar" to="lib/*.jar"/>
			</chainedmapper>
		</mapper>
	</pathconvert>
	 
    <path id="project.classpath">
		<pathelement path="${build.dir}/"/>
		<fileset refid="files.jar"/>
    </path>

    <target name="compile">
        <mkdir dir="${build.dir}"/>
        <javac 	srcdir="."
				destdir="${build.dir}"
				classpathref="project.classpath"
				includeantruntime="false"
				encoding="UTF-8"/>
    </target>

	<target name="jar" depends="compile" >
        <mkdir dir="${jar.dir}"/>
		<jar destfile="${jar.dir}/${ant.project.name}.jar" basedir="${build.dir}">
            <manifest>
                <attribute name="Main-Class" value="${main-class}"/>
                <attribute name="Class-Path" value="${manifest.classpath}"/>
            </manifest>
		</jar>
	</target>

	<target name="clean">
		<delete dir="${build.dir}" />
	</target>

</project>
